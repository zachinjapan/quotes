const incrementAction = {
  type: "INCREMENT",
};
const resetAction = {
  type: "RESET",
};

const ROUND_OVER = {
  type: "ROUND_OVER",
};

const ROUND_START = {
  type: "ROUND_START",
};

const changeQuoteTypeToFamous = {
  type: "CHANGE_QUOTE_TYPE_TO_FAMOUS",
};

const changeQuoteTypeToMovie = {
  type: "CHANGE_QUOTE_TYPE_TO_MOVIE",
};

export default {
  incrementAction,
  resetAction,
  ROUND_OVER,
  ROUND_START,
  changeQuoteTypeToFamous,
  changeQuoteTypeToMovie,
};
