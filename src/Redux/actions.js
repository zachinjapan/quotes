const incrementAction = {
  type: "INCREMENT",
};
const resetAction = {
  type: "RESET",
};

const ROUND_OVER = {
  type: "ROUND_OVER",
};

const ROUND_START = {
  type: "ROUND_START",
};

export default {
  incrementAction,
  resetAction,
  ROUND_OVER,
  ROUND_START,
};
